server {
        listen 80 default server;
        root /home/box/web;

        location /uploads/ {
            root /home/box/web/uploads;
        }
        location ~* /\.\w+$ {
            root /home/box/web/public
        }
        location ~* /\w+$ {
            root /dev/null
        }
}
